#!/bin/sh

BUILD_DIR=${1:-}

if [ -d $BUILD_DIR/build/web ]; then
    cd $BUILD_DIR/build/web
    cat <<EOF
---
default_process_types:
    web: python3 -m http.server

EOF

else
    cd $BUILD_DIR
    cat <<EOF
---
default_process_types:
    web: python3 -m http.server

EOF
fi
