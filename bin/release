#!/bin/sh

BUILD_DIR=${1:-}

if [ -d $BUILD_DIR/build/web ]; then
    cd $BUILD_DIR/build/web
else
    cd $BUILD_DIR
fi

cat <<EOF
---
default_process_types:
    web: /app/dart-sdk/bin/dart pub global run dhttpd --path /app --port ${PORT}

EOF
